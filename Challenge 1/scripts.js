const value = 3 // This number should be a numerical value, not a string.
console.log(value + 4 + value)

//JavaScript combined the values as strings, resulting in "343" instead of performing numeric addition beacuse it prioritizes string concatenation
